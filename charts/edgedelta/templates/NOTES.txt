1. Visit https://app.edgedelta.com
{{- if .Values.apiKey }}
2. Find the configuration with {{ .Values.apiKey | quote }} on https://app.edgedelta.com to check if agents are active
{{- else if and (and .Values.secretApiKey.name .Values.secretApiKey.key) .Values.secretApiKey.value }}
{{ $lookupResult := lookup "v1" "Secret" .Release.Namespace .Values.secretApiKey.name }}
{{- if $lookupResult }}
2. Find the configuration with {{ index $lookupResult.data .Values.secretApiKey.key | b64dec | quote }} on https://app.edgedelta.com to check if agents are active
{{- else }}
2. Find the configuration with {{ .Values.secretApiKey.value }} on https://app.edgedelta.com to check if agents are active
{{- end }}
{{- end }}